# Lessons Learned

## Project-Specific
- Site runs on port 3001 (not default 3000)
- GitHub username is `tstanmay13`
- Uses Tailwind CSS 4 with PostCSS plugin (not v3 config)
- No test suite configured
- Uses `styled-jsx` in some components (games page)
